# `ScrubJay.Functional.Asp`

`Asp`-related extensions to `ScrubJay.Functional`.

Most of these extensions are conversion methods between the Asp responses of 
`IActionResult`, `ActionResult`, `ActionResult<T>`, and `IResult`
with `Result`, `Result<T>`, and `Result<T,E>`.